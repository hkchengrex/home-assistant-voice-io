# Security and privacy

Report vulnerabilities privately to the repository owner before public
disclosure. Do not attach real voice recordings, credentials, webhook IDs, or
home-network details to public issues.

The Studio binds to loopback by default. Exposing it on a network changes the
threat model because it can edit the training library and access saved audio.

{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":"<p>Offline \u00b7 trainable \u00b7 cross-platform</p> A voice interface that stays home. <p>       Teach your own short commands, recognize them locally, and connect the result       to any automation. No transcription, cloud account, or fixed language.     </p> Train your first command View the repository Local signal / 16 kHz listening Lights oncommand accepted 87 ms 01 / Train Say it your way <p>Record examples in the browser-based Studio. Any language or short phrase works.</p> 02 / Listen Recognize locally <p>Use a wake phrase or listen directly. Audio stays on the machine doing the matching.</p> 03 / Act Connect anything <p>Map an accepted command to Home Assistant, another automation system, or your own code.</p>"},{"location":"#small-focused-useful","title":"Small, focused, usefulResponsive on a first-generation Surface Go.","text":"<p> Local Voice Pipeline is for repeatable commands\u2014not open-ended dictation. That narrow job makes it private, understandable, and practical on modest hardware. </p> Training Studio <p>Add phrases, compare microphones, record hands-free sets, replay takes, and review recent triggers in a local web interface.</p> <p>Open the Studio guide \u2192</p> Wake phrase or direct mode <p>Gate commands behind a trained start phrase, or classify every utterance while testing.</p> <p>Choose a listening mode \u2192</p> Automation-neutral <p>The recognizer returns intent names through a small handler contract. Your integration decides what happens next.</p> <p>Connect an automation \u2192</p> Windows, macOS, and Linux <p>The same Python package and configuration travel across platforms, with service examples for each.</p> <p>Deploy it \u2192</p> Reference benchmark <p>One 0.72-second command matched against 123 templates on an Intel Pentium Gold 4415Y.</p> See the method and full result \u2192 86.9 milliseconds median <p>What this is not</p> <p>This project does not transcribe arbitrary speech or answer open-ended questions. It recognizes the phrases you train and hands an intent to your application.</p>"},{"location":"benchmark/","title":"Benchmark","text":"<p>The reference test shows that template matching can be responsive on modest, passively cooled hardware.</p>"},{"location":"benchmark/#result","title":"Result","text":"Machine First-generation Microsoft Surface Go CPU Intel Pentium Gold 4415Y @ 1.60 GHz CPU layout 2 cores / 4 threads Runtime Python 3.12 with the native matcher Templates 123 Input phrase 0.720 seconds Measured runs 100 after 10 warm-ups Median 86.862 ms p95 91.863 ms Mean 88.266 ms Range 84.600\u2013129.362 ms"},{"location":"benchmark/#what-was-measured","title":"What was measured","text":"<p>Each run processed one stored command from silence trimming through PCEN/MFCC feature extraction, dynamic-time-warping comparison, and final command classification. The accepted result was <code>lights_on</code>.</p> <p>The test excludes microphone capture time, response playback, and external automation latency. In other words, it measures recognition after the utterance is available\u2014not the time a person spends speaking.</p>"},{"location":"benchmark/#live-observation","title":"Live observation","text":"<p>In the same running deployment, the two most recent successful commands reached the Home Assistant action 150\u2013190 ms after the detected command ended, with a 170 ms median. This is a small operational observation, not a controlled end-to-end benchmark.</p>"},{"location":"benchmark/#read-results-carefully","title":"Read results carefully","text":"<p>Latency changes with phrase duration, number of templates, native versus NumPy matching, system load, and the speed of the connected automation. Compare systems with the same recordings and template library when evaluating changes.</p>"},{"location":"configuration/","title":"Configuration","text":"<p><code>commands.toml</code> describes what can be recognized and how the local listener behaves. It should contain no credentials or private automation endpoints.</p>"},{"location":"configuration/#minimal-example","title":"Minimal example","text":"<pre><code>[recognizer]\nsample_rate = 16000\nmax_distance = 3.6\nmin_margin = 0.03\ntop_k = 2\n\n[start_phrase]\nenabled = true\nutterance = \"Hello house\"\ntarget_samples = 15\ncommand_timeout_seconds = 3.0\n\n[commands.lights_on]\ndescription = \"Turn on the room lights\"\nutterance = \"Lights on\"\ntarget_samples = 10\nintent_group = \"day_lighting\"\nresponse = \"lights_on\"\n\n[responses]\nlights_on = \"lights_on\"\n</code></pre>"},{"location":"configuration/#recognition-settings","title":"Recognition settings","text":"Setting Purpose Default <code>sample_rate</code> Audio sample rate used for recordings and matching. <code>16000</code> <code>max_distance</code> Largest template distance that can be accepted. Lower is stricter. <code>4.0</code> <code>min_margin</code> Required separation from the runner-up. Higher is stricter. <code>0.12</code> <code>top_k</code> Number of nearest templates combined for each command. <code>3</code> <p>Use <code>local-voice calibrate</code> to choose a sensible starting distance for your own recordings.</p>"},{"location":"configuration/#commands","title":"Commands","text":"<p>Each <code>[commands.name]</code> entry has:</p> <ul> <li><code>utterance</code>: the phrase shown while recording.</li> <li><code>description</code>: a human-readable explanation.</li> <li><code>target_samples</code>: the desired number of examples.</li> <li><code>intent_group</code>: the logical action shared by alternate phrases.</li> <li><code>response</code>: an optional key from <code>[responses]</code>.</li> </ul> <p>Names, intent groups, and response keys may contain letters, numbers, <code>_</code>, and <code>-</code>.</p>"},{"location":"configuration/#start-phrase","title":"Start phrase","text":"<p>The <code>[start_phrase]</code> section has its own distance, margin, and <code>top_k</code> because wake recognition is a different decision from command recognition. It also controls:</p> <ul> <li><code>command_timeout_seconds</code>: how long to wait for the command after waking.</li> <li><code>min_audio_seconds</code> and <code>max_audio_seconds</code>: accepted wake-phrase duration.</li> <li><code>min_command_audio_seconds</code>: shortest command evaluated after waking.</li> <li><code>response</code>: optional local acknowledgment.</li> </ul>"},{"location":"configuration/#calibration-and-negative-examples","title":"Calibration and negative examples","text":"<p><code>[calibration]</code> describes unrelated phrases used to measure rejection. Record varied speech under <code>_not_command</code>; these are not commands and never trigger an action.</p>"},{"location":"configuration/#local-control-events","title":"Local control events","text":"<p><code>[control_events]</code> can map loopback-only HTTP paths to response groups when <code>run --control-port</code> is enabled:</p> <pre><code>[control_events]\n\"/welcome\" = \"welcome\"\n</code></pre> <p>The server binds to <code>127.0.0.1</code>. Treat it as local process coordination, not a network API.</p>"},{"location":"deployment/","title":"Deploy on your platform","text":"<p>First get recognition working interactively. Then run the same command as a background user service with an explicit config, recordings folder, and assets folder.</p> <p>The repository includes starting points under <code>deploy/</code>:</p> Platform Example Best for Linux <code>deploy/systemd/local-voice.service</code> A user-level systemd service macOS <code>deploy/launchd/io.localvoice.pipeline.plist</code> A per-user launch agent Windows <code>deploy/windows/run-local-voice.ps1</code> A Task Scheduler action"},{"location":"deployment/#common-deployment-shape","title":"Common deployment shape","text":"<p>Keep code and personal data separate:</p> <pre><code>application checkout or installed package\nprivate data folder/\n\u251c\u2500\u2500 commands.toml\n\u251c\u2500\u2500 recordings/\n\u2514\u2500\u2500 assets/\n</code></pre> <p>Use absolute paths in the service definition. Run it as an ordinary user with access to the audio device; root or administrator access should not be necessary.</p>"},{"location":"deployment/#linux","title":"Linux","text":"<p>Copy the example unit to <code>~/.config/systemd/user/</code>, replace its working paths, then enable it as a user service. PipeWire or PulseAudio must be available in the same user session.</p>"},{"location":"deployment/#macos","title":"macOS","text":"<p>Copy the example property list to <code>~/Library/LaunchAgents/</code>, replace every placeholder, and make sure the executable has microphone permission. Test the exact command in Terminal before loading the agent.</p>"},{"location":"deployment/#windows","title":"Windows","text":"<p>Adjust the example PowerShell script to your checkout and private data folder. In Task Scheduler, run it only when the intended user is logged on so the process can use that user's audio device.</p>"},{"location":"deployment/#safe-updates","title":"Safe updates","text":"<p>Pin deployments to a reviewed release or commit. Before switching versions:</p> <ol> <li>Back up private configuration and recordings.</li> <li>Install the new package in a fresh virtual environment.</li> <li>Run a one-off recognition test.</li> <li>Restart the service and inspect its first few accepted and rejected events.</li> </ol> <p>Never copy repository credentials or private SSH keys to an always-on voice device. Deploy from a trusted development machine.</p>"},{"location":"getting-started/","title":"Train your first command","text":"<p>You can go from a fresh checkout to a tested local command in about ten minutes.</p>"},{"location":"getting-started/#what-you-need","title":"What you need","text":"<ul> <li>Windows, macOS, or Linux</li> <li>Python 3.11\u20133.13</li> <li>A microphone</li> <li>A quiet place for the first recording set</li> </ul> <p>The package is not on PyPI yet. While the repository is private, install it from a local checkout.</p>"},{"location":"getting-started/#1-create-an-environment","title":"1. Create an environment","text":"Windows PowerShellmacOS / Linux <pre><code>py -3.13 -m venv .venv\n.\\.venv\\Scripts\\Activate.ps1\npython -m pip install -e \".[capture]\"\n</code></pre> <pre><code>python3 -m venv .venv\nsource .venv/bin/activate\npython -m pip install -e \".[capture]\"\n</code></pre> <p>The <code>capture</code> extra installs microphone support. Matching existing WAV files only needs the base package.</p>"},{"location":"getting-started/#2-make-a-private-data-folder","title":"2. Make a private data folder","text":"<p>Copy the example <code>commands.toml</code> into a folder that will hold your personal configuration:</p> <pre><code>voice-data/\n\u251c\u2500\u2500 commands.toml\n\u251c\u2500\u2500 recordings/\n\u2514\u2500\u2500 assets/\n</code></pre> <p>Keep this folder out of a public repository. Voice recordings are personal data.</p>"},{"location":"getting-started/#3-check-your-microphone","title":"3. Check your microphone","text":"<pre><code>local-voice devices\n</code></pre> <p>Note the number or name of the microphone you want to use. If your default device is correct, you can leave the device option unset.</p>"},{"location":"getting-started/#4-open-the-training-studio","title":"4. Open the Training Studio","text":"<pre><code>local-voice --config voice-data/commands.toml --recordings voice-data/recordings studio\n</code></pre> <p>Open <code>http://127.0.0.1:8765</code>. Choose a phrase, select a microphone, and record at least two examples. Ten varied examples per phrase is a better starting point for everyday use.</p>"},{"location":"getting-started/#5-test-one-command","title":"5. Test one command","text":"<pre><code>local-voice --config voice-data/commands.toml --recordings voice-data/recordings listen\n</code></pre> <p>A successful test prints the best match, its intent, and the confidence margin. A rejection is safe: no command is sent.</p>"},{"location":"getting-started/#6-calibrate","title":"6. Calibrate","text":"<p>After training at least two commands with two examples each:</p> <pre><code>local-voice --config voice-data/commands.toml --recordings voice-data/recordings calibrate\n</code></pre> <p>Use the suggested midpoint as a starting value for <code>recognizer.max_distance</code>. If command classes overlap, add more varied recordings or choose phrases that sound less alike.</p>"},{"location":"getting-started/#next","title":"Next","text":"<ul> <li>Learn the Training Studio</li> <li>Run with a wake phrase</li> <li>Connect accepted commands to an automation</li> </ul>"},{"location":"integrations/","title":"Connect an automation","text":"<p>Recognition and automation are deliberately separate. The library accepts a spoken command name; your adapter decides whether that means a Home Assistant service call, a local script, or something else.</p>"},{"location":"integrations/#the-handler-contract","title":"The handler contract","text":"<p>A handler receives the accepted command name and returns an <code>ActionResult</code>:</p> <pre><code>from ha_voice import ActionResult\nfrom ha_voice.cli import main\n\n\ndef handle_command(command_name: str) -&gt; ActionResult:\n    if command_name == \"lights_on\":\n        # Call your automation here.\n        return ActionResult(status=\"sent\")\n    return ActionResult(status=\"unassigned\")\n\n\nmain(command_handler=handle_command)\n</code></pre> <p>Possible statuses are:</p> Status Meaning <code>sent</code> The adapter accepted and dispatched the command. <code>unassigned</code> Recognition succeeded, but the adapter has no action for it. <code>failed</code> The adapter tried and could not complete the action. <p>Set <code>suppress_feedback=True</code> when the adapter should prevent local response playback.</p>"},{"location":"integrations/#keep-platform-details-outside-the-library","title":"Keep platform details outside the library","text":"<p>Store URLs, tokens, entity IDs, and household mappings in the consuming application\u2014not in this repository or its public command examples. A clean adapter usually has three parts:</p> <ol> <li>A private map from recognized command names to automation actions.</li> <li>A small client that sends the action and handles timeouts.</li> <li>A handler that converts the outcome to <code>ActionResult</code>.</li> </ol>"},{"location":"integrations/#home-assistant","title":"Home Assistant","text":"<p>Use Home Assistant's authenticated API, a webhook owned by your private deployment, or another local integration. Keep the exact endpoint and entity mappings in the private Home Assistant repository. The voice library should know only stable intent names such as <code>lights_on</code> or <code>good_night</code>.</p> <p>Group alternate wording</p> <p>If \u201clights on\u201d and another phrase should perform the same action, give both commands the same <code>intent_group</code>. Your adapter can then map the logical intent without weakening recognition between aliases.</p>"},{"location":"privacy/","title":"Privacy and data ownership","text":"<p>Local Voice Pipeline processes recordings on the machine where it runs. The library has no cloud recognition service and does not need an account.</p>"},{"location":"privacy/#what-stays-local","title":"What stays local","text":"<ul> <li>Training recordings</li> <li>Wake-phrase and negative examples</li> <li>Recent diagnostic trigger captures</li> <li>Optional response audio</li> <li>Your command configuration</li> </ul> <p>The integration you attach may communicate with another system. Review that adapter separately; the core recognizer does not make an external action by itself.</p>"},{"location":"privacy/#treat-recordings-as-personal-data","title":"Treat recordings as personal data","text":"<p>A voice recording can identify or reveal information about a person. Store the data folder privately, restrict access to the service user, and include recordings only in encrypted or otherwise protected backups.</p> <p>Do not commit real household recordings, private response assets, automation tokens, entity IDs, or local hostnames to a public repository. Public tests should synthesize audio or use recordings with clear redistribution permission.</p>"},{"location":"privacy/#diagnostic-retention","title":"Diagnostic retention","text":"<p>Wake-phrase mode stores a bounded queue of recent triggers for review. The current default keeps up to 20 trigger events. Dismiss unneeded events in the Studio, and periodically review the recordings folder as part of normal maintenance.</p>"},{"location":"privacy/#before-sharing-a-bug-report","title":"Before sharing a bug report","text":"<p>Prefer logs, sanitized thresholds, and synthetic reproductions. If audio is essential, trim it to the minimum needed, remove unrelated speech, and share it only through a channel appropriate for personal data.</p>"},{"location":"running/","title":"Run continuously","text":"<p>Continuous mode watches the microphone, detects an utterance, classifies it, and optionally plays a local response. Your application can attach an action handler for accepted commands.</p>"},{"location":"running/#wake-phrase-mode","title":"Wake-phrase mode","text":"<p>This is the normal mode for an always-on device. Train the <code>_start_phrase</code> set, enable <code>[start_phrase]</code> in <code>commands.toml</code>, then run:</p> <pre><code>local-voice --config voice-data/commands.toml --recordings voice-data/recordings run --assets voice-data/assets\n</code></pre> <p>The listener waits for the start phrase, plays its response if configured, and opens a short window for one command.</p> <p>Before relying on it:</p> <ol> <li>Record at least the configured <code>target_samples</code> for the start phrase.</li> <li>Record unrelated speech for <code>_not_command</code> and several non-wake phrases for rejection.</li> <li>Test from realistic positions and with typical background noise.</li> <li>Review recent triggers in the Studio and promote false triggers as negative examples.</li> </ol>"},{"location":"running/#direct-mode","title":"Direct mode","text":"<p>Direct mode classifies every detected utterance and disables actions. It is useful for testing commands or collecting fresh examples:</p> <pre><code>local-voice --config voice-data/commands.toml --recordings voice-data/recordings run --direct\n</code></pre> <p>To save five new test utterances as examples for one command:</p> <pre><code>local-voice --config voice-data/commands.toml --recordings voice-data/recordings run --direct --capture-command lights_on --capture-count 5\n</code></pre>"},{"location":"running/#input-and-output-devices","title":"Input and output devices","text":"<p>Pass a device number or name with <code>--input-device</code> and <code>--output-device</code>. Run <code>local-voice devices</code> to see available inputs.</p> <p>If the listener misses quiet speech, inspect the microphone first. <code>--min-rms</code> and <code>--noise-multiplier</code> tune voice activity detection, but very aggressive settings can increase false triggers.</p>"},{"location":"running/#local-responses","title":"Local responses","text":"<p>The <code>[responses]</code> section maps a response group to an audio filename prefix. Put matching audio files in the assets folder passed to <code>run</code>. A command can name a response group, or leave it blank for no local playback.</p> <p>Responses are optional and separate from the external action. Your handler can also suppress feedback when an action fails or already provides its own confirmation.</p>"},{"location":"training-studio/","title":"Training Studio","text":"<p>The Studio is a local browser interface for building and improving a voice library. It runs on your machine; it is not a hosted recording service.</p> <p>Start it with:</p> <pre><code>local-voice --config voice-data/commands.toml --recordings voice-data/recordings studio\n</code></pre> <p>Then open <code>http://127.0.0.1:8765</code>.</p>"},{"location":"training-studio/#build-a-useful-recording-set","title":"Build a useful recording set","text":"<p>Choose a command from the left side, select your microphone, and record examples. You can keep or retry each take, replay saved takes, replace weak recordings, and remove mistakes.</p> <p>For hands-free collection, choose Record a full set. A chime starts each take and the Studio saves it automatically.</p> <p>Good training examples include normal variation:</p> <ul> <li>Stand where you will actually use the command.</li> <li>Vary your distance and speaking pace a little.</li> <li>Include normal room noise, but avoid clipping or very low volume.</li> <li>Start with about 10 recordings per command and 15 for the wake phrase.</li> </ul>"},{"location":"training-studio/#add-commands","title":"Add commands","text":"<p>Choose Add command, then provide a stable command name, the phrase to display, and a short description. The Studio adds it to your private <code>commands.toml</code> and creates its recording set.</p> <p>Use <code>intent_group</code> when two different spoken phrases should trigger the same logical action. That keeps aliases from competing against each other during acceptance.</p>"},{"location":"training-studio/#test-without-triggering-actions","title":"Test without triggering actions","text":"<p>One-off test records and classifies one phrase. Start continuous listens repeatedly. Both Studio test modes deliberately avoid sending external actions, so you can evaluate recognition safely.</p>"},{"location":"training-studio/#compare-microphones","title":"Compare microphones","text":"<p>Compare microphones alternates between two input devices while you hold the same position. Replay each pair and choose the device with clearer, more consistent speech\u2014not simply the loudest signal.</p>"},{"location":"training-studio/#review-false-triggers","title":"Review false triggers","text":"<p>In wake-phrase mode, the listener keeps a bounded set of recent accepted triggers. The Studio lets you replay them, dismiss expected results, or promote false triggers into negative examples. Those examples help the recognizer reject similar sounds in the future.</p> <p>The Studio can change your library</p> <p>Adding or removing commands and takes edits files in your data folder. Back up <code>commands.toml</code> and recordings privately if the library is important.</p>"},{"location":"troubleshooting/","title":"Troubleshooting","text":""},{"location":"troubleshooting/#no-microphone-appears","title":"No microphone appears","text":"<p>Run <code>local-voice devices</code>. If the list is empty:</p> <ul> <li>Confirm the operating system has granted microphone access to the terminal or Python process.</li> <li>On Linux, confirm PipeWire or PulseAudio is running for the same user.</li> <li>Reinstall the capture extra: <code>python -m pip install -e \".[capture]\"</code>.</li> <li>Disconnect and reconnect USB audio devices, then list devices again.</li> </ul>"},{"location":"troubleshooting/#the-right-phrase-is-rejected","title":"The right phrase is rejected","text":"<p>Start with the recordings before changing thresholds:</p> <ol> <li>Replay several saved takes in the Studio.</li> <li>Remove clipped, cut-off, unusually quiet, or mislabeled takes.</li> <li>Add examples from the real speaking distance and room.</li> <li>Run <code>local-voice calibrate</code> again.</li> </ol> <p>Then consider raising <code>max_distance</code> slightly or lowering <code>min_margin</code>. Looser settings can also increase false accepts, so test unrelated speech afterward.</p>"},{"location":"troubleshooting/#the-wrong-command-wins","title":"The wrong command wins","text":"<p>Choose phrases that sound more distinct, add varied examples to both commands, and remove ambiguous takes. If the phrases are alternate wording for the same action, give them the same <code>intent_group</code>.</p>"},{"location":"troubleshooting/#the-wake-phrase-triggers-accidentally","title":"The wake phrase triggers accidentally","text":"<p>Open Recent triggers in the Studio, replay the capture, and promote genuine false triggers as negative examples. Also record varied <code>_not_command</code> examples and avoid a wake phrase that resembles common background speech.</p>"},{"location":"troubleshooting/#continuous-listening-stops","title":"Continuous listening stops","text":"<p>Run the exact service command interactively and watch the output. Common causes are a missing audio device after reboot, a config path relative to the wrong working directory, or a response asset that cannot be played.</p>"},{"location":"troubleshooting/#matching-is-slower-than-expected","title":"Matching is slower than expected","text":"<p>Check whether the optional native matcher built successfully. Performance also scales with the number and length of templates. Keep useful variation, but remove duplicates and poor-quality takes rather than growing the library indefinitely.</p>"},{"location":"troubleshooting/#get-useful-diagnostic-information","title":"Get useful diagnostic information","text":"<p>When reporting a problem, include:</p> <ul> <li>Operating system and Python version</li> <li>CPU model</li> <li>Template counts from <code>local-voice ... templates</code></li> <li>Whether matching uses the native or NumPy backend</li> <li>Sanitized configuration thresholds</li> <li>The printed distance, margin, duration, and matching time</li> </ul> <p>Do not attach personal recordings publicly unless every speaker has explicitly agreed.</p>"},{"location":"reference/cli/","title":"Command line","text":"<p>Global options come before the subcommand:</p> <pre><code>local-voice --config PATH --recordings PATH COMMAND [OPTIONS]\n</code></pre> Command What it does <code>devices</code> Lists available audio input devices. <code>record NAME</code> Records template WAV files for one command or special set. <code>listen</code> Captures and classifies one utterance. <code>match WAV</code> Classifies an existing WAV file. <code>templates</code> Shows the number of recordings for each command. <code>calibrate</code> Measures same-command and other-command distance separation. <code>studio</code> Starts the local Training Studio at port 8765 by default. <code>run</code> Runs continuous wake-phrase or direct listening. <p>Use the built-in help for the exact options in your installed version:</p> <pre><code>local-voice --help\nlocal-voice record --help\nlocal-voice run --help\n</code></pre>"},{"location":"reference/cli/#special-recording-sets","title":"Special recording sets","text":"<ul> <li><code>_start_phrase</code> contains examples of the configured wake phrase.</li> <li><code>_not_command</code> contains varied speech that should be rejected.</li> </ul> <p>These names are configured by the library and are not external automation actions.</p>"},{"location":"reference/cli/#exit-behavior","title":"Exit behavior","text":"<p><code>listen</code> and <code>match</code> exit with status 0 for an accepted command and status 2 for a safe rejection. Configuration and missing-library problems stop with an explanatory error.</p>"},{"location":"reference/python-api/","title":"Python API","text":"<p>The stable top-level API exposes configuration, templates, classification results, and the automation handler contract:</p> <pre><code>from ha_voice import (\n    ActionResult,\n    AppConfig,\n    CommandConfig,\n    CommandHandler,\n    MatchResult,\n    RecognizerConfig,\n    Template,\n    classify,\n    load_config,\n    load_templates,\n)\n</code></pre>"},{"location":"reference/python-api/#configuration-and-templates","title":"Configuration and templates","text":"<pre><code>from pathlib import Path\nfrom ha_voice import load_config, load_templates\n\nconfig = load_config(Path(\"voice-data/commands.toml\"))\ntemplates = load_templates(\n    Path(\"voice-data/recordings\"),\n    config.recognizer.sample_rate,\n)\n</code></pre> <p><code>load_config</code> validates names, ranges, references, and the presence of at least one command. <code>load_templates</code> reads the private WAV library and extracts the data needed for matching.</p>"},{"location":"reference/python-api/#classification","title":"Classification","text":"<p><code>classify</code> accepts command features, a template collection, and the recognizer thresholds. It returns a <code>MatchResult</code> with the accepted command, score, margin, per-command scores, and acceptance flag.</p> <p>For most applications, the continuous CLI already owns capture, wake gating, feedback, and diagnostics. Supply a <code>CommandHandler</code> to <code>ha_voice.cli.main</code> rather than reimplementing that loop.</p>"},{"location":"reference/python-api/#compatibility-boundary","title":"Compatibility boundary","text":"<p>The <code>ha_voice</code> import namespace and top-level action/configuration contracts are the intended public boundary. Lower-level audio, feature, Studio, listener, and service modules are available for advanced use but may evolve more quickly before version 1.0.</p>"}]}